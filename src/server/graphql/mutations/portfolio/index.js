import addPortfolio from '<server/graphql>/mutations/portfolio/add';
import updatePortfolio from '<server/graphql>/mutations/portfolio/update';
import removePortfolio from '<server/graphql>/mutations/portfolio/remove';

export default {
    addPortfolio,
    updatePortfolio,
    removePortfolio,
};
