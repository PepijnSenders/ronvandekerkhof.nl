import addPublicity from '<server/graphql>/mutations/publicity/add';
import updatePublicity from '<server/graphql>/mutations/publicity/update';
import removePublicity from '<server/graphql>/mutations/publicity/remove';

export default {
    addPublicity,
    updatePublicity,
    removePublicity,
};
