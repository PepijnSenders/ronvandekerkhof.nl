import addDate from '<server/graphql>/mutations/date/add';
import updateDate from '<server/graphql>/mutations/date/update';
import removeDate from '<server/graphql>/mutations/date/remove';

export default {
    addDate,
    updateDate,
    removeDate,
};
