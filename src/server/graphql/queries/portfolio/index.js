import single from '<server/graphql>/queries/portfolio/single';
import multiple from '<server/graphql>/queries/portfolio/multiple';

export default {
    portfolio: single,
    portfolios: multiple,
};
